# XtremeIdiots.Portal.ChatCommands.Abstractions.V1

Shared contract types and constants for chat command configuration and metadata.

This package is owned by `portal-server-events` and consumed by other portal repositories.
